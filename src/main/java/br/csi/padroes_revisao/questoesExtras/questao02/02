Desenvolver um framework capaz de gerar Builders automaticamente
para qualquer classe utilizando Reflection.

O Builder deverá:
- identificar propriedades automaticamente;
- criar métodos set<Property>();
- permitir construção fluente;
- retornar o objeto montado através do método build().

Deverá ser implementado o padrão Builder.